{
  "name": "shared",
  "version": "0.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "source": "./src/index.ts",
  "private": true,
  "files": [
    "dist/**"
  ],
  "scripts": {
    "dev": "tsc -w",
    "build": "tsc",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "devDependencies": {
    "tsconfig": "*",
    "typescript": "^4.5.3"
  },
  "dependencies": {
    "mongoose": "^6.3.4",
    "typescript": "^4.5.3"
  }
}
